# DNS Records to replicate from drownedashes.com to torrift.com

## Required Records (from drownedashes.com):

- A: torrift.com -> <DDNS-IP>    # Set to dynamic IP, update via script
- A: www.torrift.com -> <DDNS-IP> # Set to same as bare domain or CNAME to torrift.com
- MX: (none found for drownedashes.com; set if needed)
- AAAA: (none found)
- CNAME: (none found for www, so create if needed)

## Notes
- Use Cloudflare API to automate creation/updating of these records.
- DDNS: Set up A records for both root (torrift.com) and www (www.torrift.com), which update automatically as IP changes.
- Future records can be added as needed.